export enum UserType {
  ADMIN = 'ADMIN',
  DEFAULT = 'DEFAULT',
  CADASTRE = 'CADASTRE',
  REPORT = 'REPORT',
}

export enum UnitType {
  KG = 'KG',
  G = 'G',
  L = 'L',
  UN = 'UN',
}

export enum ProductType {
  DONATED = 'DONATED',
  PURCHASED = 'PURCHASED',
}

export enum ReportType {
  VALIDITY = "VALIDITY",
  DONATIONS = "DONATIONS",
  PURCHASED = "PURCHASED",
  INVENTORY = "INVENTORY",
}